<?php

namespace Kedniko\Vivy\Core;

/**
 * Classe helper per impostare il validatore
 *
 * Alcuni field potrebbero avere delle rule applicate\
 * di default come `required`/`notNull`/etc..
 */
final class GroupBuilder
{
    // use TraitUserDefinedCallGroup;

    // // protected $types;

    // /** @var LinkedList $types */
    // protected $types;

    // public function __construct($setup = null)
    // {
    // 	$this->fields = new LinkedList;

    // 	if (!$setup) {
    // 		return;
    // 	}

    // 	// if (is_callable($setup)) {
    // 	// 	$setup($this);
    // 	// 	return;
    // 	// }

    // 	if (is_array($setup)) {
    // 		$this->applyArraySetup($setup);
    // 	}
    // }
}
